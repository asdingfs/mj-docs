# Documentation work

Read [context/README.md](context/README.md), then the task-relevant playbook before editing.

- `../mj-be` is a read-only reference. Do not run builds, tests, servers, formatters, migrations, or installs there.
- Stage proposed documentation outside this checkout. Show a rendered preview and exact diff; apply only the version the owner approves.
- Preserve existing GitBook space keys, URLs, and assets. Keep `context/` outside published roots.
- Maintain context during every documentation task using [context/maintenance.md](context/maintenance.md). Do not accumulate session transcripts or duplicate instructions.
